

// In Dart, you cannot use a variable before it is declared.
// If you try to do so, the compiler will throw a compilation error (e.g., "Undefined name").
// Dart follows a top-down execution and requires variables to be defined in the scope before they are accessed.
